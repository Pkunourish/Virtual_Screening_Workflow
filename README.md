# Virtual_Screening_Workflow

a Python-based workflow for data processing after virtual screening. Vina and Glide results as example. Codes are provided with .ipynb and .py format. 

Detailed footnotes are provided within code files.

Carried out in following order: vina/glide → merge → RMSD → PharmFit → SASA → pains → cluster

Random generated result files are provided as example.
